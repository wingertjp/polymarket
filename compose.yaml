services:
  snipe:
    build: .
    command: python snipe.py
    env_file:
      - .env
    network_mode: host
    restart: unless-stopped

  redeem:
    build: .
    command: python redeem.py
    env_file:
      - .env
    network_mode: host
    restart: unless-stopped
